<Page
    x:Class="Client.Views.SeventhPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Client.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>

                <VisualState x:Name="small">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="tbStatus.Text" Value="small"/>
                        <Setter Target="gridView.Margin" Value="12,0,2,0"/>
                        <Setter Target="gridItems.MaximumRowsOrColumns" Value="1"/>
                        <Setter Target="appBar.(Grid.Row)" Value="2"/>
                    </VisualState.Setters>
                </VisualState>

                <VisualState x:Name="medium">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="641"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="tbStatus.Text" Value="medium"/>
                        <Setter Target="gridView.Margin" Value="24,0,14,0"/>
                        <Setter Target="gridItems.MaximumRowsOrColumns" Value="2"/>
                        <Setter Target="appBar.(Grid.Row)" Value="0"/>
                    </VisualState.Setters>
                </VisualState>

                <VisualState x:Name="large">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1008"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="tbStatus.Text" Value="large"/>
                        <Setter Target="gridView.Margin" Value="24,0,14,0"/>
                        <Setter Target="gridItems.MaximumRowsOrColumns" Value="3"/>
                        <Setter Target="appBar.(Grid.Row)" Value="0"/>
                    </VisualState.Setters>
                </VisualState>

            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>


        <GridView Grid.Row="1" x:Name="gridView" SizeChanged="OnSizeChanged">
            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <VariableSizedWrapGrid x:Name="gridItems"
                                               MaximumRowsOrColumns="{Binding MaximumRowsOrColumns, Mode=TwoWay}"
                                               Orientation="Horizontal" ItemHeight="400" ItemWidth="{Binding ItemWidth, Mode=OneWay, FallbackValue=200}"/>
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>
            <GridView.ItemContainerStyle>
                <Style TargetType="GridViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                    <Setter Property="Margin" Value="0,0,10,10"/>
                    <Setter Property="Padding" Value="0"/>
                </Style>
            </GridView.ItemContainerStyle>
            <Rectangle Fill="Red"/>
            <Rectangle Fill="Green"/>
            <Rectangle Fill="Blue"/>
            <Rectangle Fill="Yellow"/>
            <Rectangle Fill="Pink"/>
            <Rectangle Fill="Purple"/>
        </GridView>

        <TextBlock Grid.Row="1" x:Name="tbStatus" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="30"/>

        <CommandBar x:Name="appBar">
            <CommandBar.PrimaryCommands>
                <AppBarButton Icon="Account"/>
            </CommandBar.PrimaryCommands>
        </CommandBar>
    </Grid>
</Page>
